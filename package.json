{
  "name": "fred-dashboard",
  "version": "1.0.0",
  "description": "FRED Data Dashboard",
  "scripts": {
    "build": "node scripts/build-pages.js",
    "start": "node server/server.js",
    "dev": "node --watch server/server.js",
    "deploy:worker": "npx wrangler deploy --config worker/wrangler.toml"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "xlsx": "^0.18.5"
  }
}
